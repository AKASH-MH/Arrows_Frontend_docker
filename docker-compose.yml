services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3000/api}
    container_name: methodhub-frontend
    ports:
      - "${FRONTEND_PORT:-5173}:80"
    restart: unless-stopped
